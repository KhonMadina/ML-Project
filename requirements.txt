# Reproducible environment for Khmer Sentiment Analysis Pipeline
# Compatible with Python 3.8 through 3.13 using environment markers.
# - For Python < 3.9 (e.g., 3.8), use older, known-good pins.
# - For Python >= 3.9 (incl. 3.13), allow pip to choose modern wheels.

# Core runtime (Python < 3.9)
numpy==1.24.4; python_version < "3.9"
pandas==2.0.3; python_version < "3.9"
scikit-learn==1.3.2; python_version < "3.9"
joblib==1.3.2; python_version < "3.9"
scipy==1.10.1; python_version < "3.9"
matplotlib==3.7.3; python_version < "3.9"
seaborn==0.13.2; python_version < "3.9"

# Core runtime (Python >= 3.9)
# Let pip resolve compatible versions (incl. Python 3.13) without forcing source builds.
numpy>=2.0; python_version >= "3.9"
pandas>=2.2; python_version >= "3.9"
scikit-learn>=1.5; python_version >= "3.9"
joblib>=1.3; python_version >= "3.9"
matplotlib>=3.8; python_version >= "3.9"
seaborn>=0.13; python_version >= "3.9"
# Do NOT pin scipy for >=3.9; scikit-learn will pull a compatible version automatically.

# Optional: resampling used by training (--resample)
imbalanced-learn>=0.11

# Transformer stack for advanced baselines (Python >= 3.9 assumed for latest wheels)
# For Python < 3.9, you may need to pin compatible versions or use the Dockerfile.
transformers>=4.44; python_version >= "3.9"
datasets>=2.20; python_version >= "3.9"
accelerate>=0.33; python_version >= "3.9"
evaluate>=0.4; python_version >= "3.9"
# Tokenizers for custom tokenizer training
tokenizers>=0.19
# Torch: prefer official wheels; leave unpinned to let pip resolve GPU/CPU variant
# Users on Windows/CPU: pip install torch --index-url https://download.pytorch.org/whl/cpu
# Users on CUDA: follow https://pytorch.org/get-started/locally/
torch; python_version >= "3.9"

# Notebook/Dev (optional)
jupyterlab>=4
ipykernel>=6

# Reproducibility & tracking (optional but recommended)
pyyaml>=6.0
mlflow>=2.16
wandb>=0.17

# Annotation tool (optional runtime outside this repo)
# label-studio>=1.10
